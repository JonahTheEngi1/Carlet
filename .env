# Postgres credentials for docker-compose (used by backend)
POSTGRES_USER=carlet
POSTGRES_PASSWORD=carletpass
POSTGRES_DB=carletdb

# Optional Vite variables (set these only if you want to use a real external service)
VITE_BASE44_APP_ID=
VITE_BASE44_FUNCTIONS_VERSION=
VITE_BASE44_APP_BASE_URL=
